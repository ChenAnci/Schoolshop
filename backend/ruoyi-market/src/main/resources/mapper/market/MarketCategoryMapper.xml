<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.market.mapper.MarketCategoryMapper">

    <resultMap type="com.ruoyi.market.domain.MarketCategory" id="MarketCategoryResult">
        <id     property="categoryId"   column="category_id"   />
        <result property="parentId"     column="parent_id"     />
        <result property="categoryName" column="category_name" />
        <result property="sort"         column="sort"          />
        <result property="status"       column="status"        />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"        />
    </resultMap>

    <sql id="selectMarketCategoryVo">
        select category_id, parent_id, category_name, sort, status, create_by, create_time, update_by, update_time, remark
        from market_category
    </sql>

    <select id="selectCategoryList" parameterType="com.ruoyi.market.domain.MarketCategory" resultMap="MarketCategoryResult">
        <include refid="selectMarketCategoryVo"/>
        <where>
            <if test="categoryName != null and categoryName != ''"> and category_name like concat('%', #{categoryName}, '%')</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
            <if test="parentId != null"> and parent_id = #{parentId}</if>
        </where>
        order by sort
    </select>

</mapper>
