<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.market.mapper.MarketProductMapper">

    <resultMap type="com.ruoyi.market.domain.MarketProduct" id="MarketProductResult">
        <id     property="productId"    column="product_id"    />
        <result property="shopId"       column="shop_id"       />
        <result property="categoryId"   column="category_id"   />
        <result property="productName"  column="product_name"  />
        <result property="productDesc"  column="product_desc"  />
        <result property="productImage" column="product_image" />
        <result property="price"        column="price"         />
        <result property="stock"        column="stock"         />
        <result property="sales"        column="sales"         />
        <result property="status"       column="status"        />
        <result property="sort"         column="sort"          />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"        />
        <result property="categoryName" column="category_name" />
        <result property="shopName"     column="shop_name"     />
    </resultMap>

    <sql id="selectMarketProductVo">
        select p.product_id, p.shop_id, p.category_id, p.product_name, p.product_desc, p.product_image,
               p.price, p.stock, p.sales, p.status, p.sort, p.create_by, p.create_time, p.update_by, p.update_time, p.remark,
               c.category_name, s.shop_name
        from market_product p
        left join market_category c on p.category_id = c.category_id
        left join market_shop s on p.shop_id = s.shop_id
    </sql>

    <select id="selectProductList" parameterType="com.ruoyi.market.domain.MarketProduct" resultMap="MarketProductResult">
        <include refid="selectMarketProductVo"/>
        <where>
            <if test="productName != null and productName != ''"> and p.product_name like concat('%', #{productName}, '%')</if>
            <if test="categoryId != null"> and p.category_id = #{categoryId}</if>
            <if test="shopId != null"> and p.shop_id = #{shopId}</if>
            <if test="status != null and status != ''"> and p.status = #{status}</if>
        </where>
        order by p.sort
    </select>

</mapper>
